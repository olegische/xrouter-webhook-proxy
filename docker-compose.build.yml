services:
  xrouter-webhook-proxy-yc:
    image: cr.yandex/${REGISTRY_ID}/${PROJECT_NAME}:${VERSION}
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
      tags:
        - cr.yandex/${REGISTRY_ID}/${PROJECT_NAME}:${VERSION}
        - cr.yandex/${REGISTRY_ID}/${PROJECT_NAME}:latest

  xrouter-webhook-proxy:
    image: xrouter/${PROJECT_NAME}:${VERSION}
    build:
      context: .
      dockerfile: Dockerfile
      platforms:
        - linux/amd64
      tags:
        - xrouter/${PROJECT_NAME}:${VERSION}
        - xrouter/${PROJECT_NAME}:latest
